@startuml User Interface Flow
!theme plain
skinparam backgroundColor #FFFFFF

start

:Developer opens file in IDE;

if (Entry Point Choice?) then (Visual Interface)
    :Trigger analysis through\nvisual indicators;
else (Command-Based Access)
    :Trigger analysis through\ncommand interface;
endif

:YouTube IDE Extension\ninitiates analysis;

:Show progress feedback\nduring AI processing;

:LLM Best Practices Agent\nprocesses file;

:Agent returns analysis results;

:Display violations using\nfamiliar interface patterns\nwith explanations;

if (Developer interacts with\nviolation markers?) then (Yes)
    :Show violation explanation;
    :Show contextual code fix\nsuggestions;
    
    if (Developer chooses to\napply fix?) then (Yes)
        :Apply suggested fix\nto code;
    else (No)
        :Continue without\napplying fix;
    endif
else (No)
    :Violations remain visible\nin interface;
endif

if (Developer continues working?) then (Yes)
    :System manages stale state\nautomatically;
else (No)
    :Developer closes file;
endif

stop


@enduml
